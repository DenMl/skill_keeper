<tr>
  <td><%= skill.name %></td>
  <td><%= skill.description %></td>
  <td>
    <div class="btn-group btn-group-xs">
      <%= link_to "Edit", edit_skill_path(skill), remote: true, class: "btn btn-xs btn-default" %>
      <%= link_to "Delete", skill, method: :delete, remote: true, class: "btn btn-warning btn-xs" %>
    </div>
  </td>
</tr>

<% provide(:title, 'All users') %>
<div class="container">
  <h3>
    <%= yield(:title) %>
  </h3>

  <%= will_paginate %>

  <table class="table table-hover">
    <thead>
    <tr>
      <th>&nbsp;</th>
      <th>Name</th>
      <th>Email</th>
      <th>&nbsp;</th>
    </tr>
    </thead>
    <tbody>

    <% @users.each do |user| %>
        <tr>
          <td><%= gravatar_for user, size: 20 %></td>
          <td class="user_name"><%= user.name %></td>
          <td><%= user.email %></td>
          <td>
            <% if current_user.admin? && !current_user?(user) %>
              <%= link_to "Delete", user, method: :delete, data: { confirm: "You sure?" }, class: "btn btn-warning btn-xs" %>
            <% end %>
          </td>
        </tr>
    <% end %>

    </tbody>
  </table>

  <%= will_paginate %>

</div>
